import React from 'react';

const CameraView = () => {
    return (
        <div style={{ border: '2px dashed #999', padding: '50px', textAlign: 'center' }}>
            <h3>📷 Camera View Placeholder</h3>
            <p>Imagine a live camera feed here.</p>
        </div>
    );
};

export default CameraView;
